<div class="card-map-container">
        <div class="card-map">
        <h1><%= shop.name %></h1>
        <h4><%= shop.street_address %></h4>
          <%# if shop.picture.present? %>
          <%# else %>
            <p><%#= image_tag("GetNGo1.png", width: '35%') %></p>
          <%# end %>

          <% sales.each do |sale| %>
          <p><strong>Product in sale: </strong><%= sale.product_name %> <strong>for</strong> <%= sale.price %> € <strong>Terms:</strong> <%= sale.terms %></p>
        <%= link_to 'View sale details', shop_sale_path(sale.shop, sale), class: "btn btn-primary" %>
        <%= link_to 'Get map directions', "//maps.google.com/maps?daddr=#{shop.full_address}", target: '_blank', class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
