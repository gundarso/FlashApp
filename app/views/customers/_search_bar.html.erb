<%= simple_form_for :search, :method => :get do |f| %>
  <%= f.input :base_location, placeholder: 'Sales location' %>
  <%= f.input :categories, as: :check_boxes, placeholder: "Sales categories"%>
  <%= f.button :submit %>
<% end %>
